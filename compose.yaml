services:
  tcc:
    build:
      context: .
      dockerfile: docker/Dockerfile
    # o projeto inteiro é volume: editar no Windows reflete no container na hora,
    # e results/ fica no host — o dado primário não some junto com o container
    env_file: .env
    volumes:
      - .:/app
    stdin_open: true
    tty: true
